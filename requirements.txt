# Trading Platform — $100K Digital Wealth
streamlit>=1.32.0
plotly>=5.20.0
pandas>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# Robinhood official crypto API auth (Ed25519 signing)
PyNaCl>=1.5.0

# Claude AI signals (Anthropic API)
anthropic>=0.25.0

# Multi-AI consensus engine (add API keys to .env to activate each)
openai>=1.30.0            # GPT-4o-mini
google-genai>=1.0.0          # Gemini 2.5 Flash (new SDK)
groq>=0.9.0               # Llama 3.3 70B (free tier at console.groq.com)

# Browser automation (for other tools)
playwright

# Optional: faster HTTP for data fetches
httpx>=0.27.0

# Intelligent strategy features
yfinance>=0.2.40          # Historical OHLCV for pattern recognition + ML models
scikit-learn>=1.4.0       # GradientBoosting ML predictor
numpy>=1.26.0             # Required by scikit-learn + market_data
pyarrow>=14.0.0           # Parquet cache format for market_data
vaderSentiment>=3.3.2     # NLP sentiment analysis (enhances news_sentiment.py)
